function isLowerCase(string){

    if(string === string.toLowerCase()){

        return true;

    } else {

        return false;

    }

}

function isLowerCaseIndex(string, index){
    
    if(string.charAt(index) === string.charAt(index).toLowerCase()){

        return true;

    } else {

        return false;

    }

}