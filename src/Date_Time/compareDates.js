var compareDates = function (firstDate, secondDate) {

    if (firstDate > secondDate) return ("First Date > Second Date");

    else if (firstDate < secondDate) return ("Second Date > First Date");

    else return ("First Date = Second Date");

}